import { AboutSection, HeroSection, ProjectsSection } from './sections'

const Home = () => {
  return (
    <main>
      <HeroSection />

      <ProjectsSection />

      <AboutSection />
    </main>
  )
}

export default Home
